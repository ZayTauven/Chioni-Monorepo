# Chioni — SaaS de gestion des centres de santé (Comores)

## Vision

Chioni numérise la gestion des centres de santé comoriens (hôpitaux, cliniques privées) et fiabilise le financement des soins par la diaspora grâce au **« Pont de Confiance »** : le patient partage son carnet/ordonnance avec ses proches à l'étranger, qui paient **directement le centre de santé** pour un acte identifié — jamais via un intermédiaire individuel. Chaque franc est relié à un patient, un acte, un prestataire et un reçu.

**Document de référence : [docs/etude-des-besoins.md](docs/etude-des-besoins.md)** (v1.1, validé) — contexte, personas, les 3 portes d'onboarding, conception du Pont de Confiance, périmètre MVP, risques. Le lire avant toute décision produit ou d'architecture.

Deux lignes produit à garder en tête en permanence :

1. **« Aider mieux », jamais surveiller** — la dignité de tous les acteurs est une contrainte de conception.
2. **Les centres de santé sont le fond de commerce** (modèle retenu : abonnement SaaS par centre ; gratuit pour patients et tuteurs). Les gâter en fonctionnalités : gestion d'activité complète, optimisation, tableaux de bord. Le SaaS socle doit être si bon qu'un centre le garderait même sans le Pont de Confiance.

## Langue de travail

Répondre à l'utilisateur **en français**. Code, identifiants et commits en anglais ; textes UI destinés aux utilisateurs finaux en français (i18n prévue : shikomori en phase 2).

## État actuel du repo

```
Chioni/
├── backend/          # Django 5.2 + DRF — projet `config`, apps dans backend/apps/
├── frontend/         # Next.js 15 (chioni-frontend), copie de vireo template/ — builde
├── vireo template/   # référence source (LECTURE SEULE, ne jamais modifier)
└── docs/             # cadrage, décisions (ADR), spécifications
```

- **Backend complet jusqu'à la couche API/services** : venv `backend/.venv` (Python 3.13), settings via `backend/.env` (django-environ), `/api/v1/health/` public, Swagger sur `/api/docs/`, DRF deny-by-default.
  - **Modèles durcis** (revue adversariale) : append-only ORM + **triggers PostgreSQL** (ledger, reçus, audit + validation same-patient / lien révoqué définitif / facture gelée — ADR 0006), `generic_category` distinct du libellé comptable (ADR 0005), `Consent.objects.active_scopes()` = point de vérité.
  - **API phase A** : JWT + blacklist + throttling (`/api/v1/auth/*`, `/auth/me/` = routeur des 3 espaces), architecture permissions (ADR 0008 : une vue = une casquette, cloisonnement au queryset, 404 déterministe, `guardian_links_with_scope` seul chemin tuteur), 3 portes d'onboarding, consentements, fusion de doublons anti-cycle, AuditLog câblé (payload = références only). Lecture clinique segmentée par rôle (diagnostic = rôles cliniques seuls ; ordonnances + pharmacien ; staff admin = vue exploitation sans clinique) ; identité d'un profil revendiqué non modifiable par le staff. Téléphones normalisés E.164 (`apps/common/phones.py`, région KM).
  - **API phase B (Pont de Confiance)** : machine à états exclusive (`payee` uniquement via webhook signé → ledger équilibré + intent lié), abstraction PSP (`psp/fake.py` HMAC ; `psp/stripe.py` drop-in à implémenter — garde-fou boot : fake interdit si DEBUG=False), FX EUR→KMF à taux figé (devis avant paiement, frais 2,50 % en sus, le centre reçoit 100 % en KMF), reçus double devise relus depuis le ledger, litiges (`Dispute`), accusé patient stocké (indicateur de mission). ADR 0009.
  - **Notifications SMS métier** (ADR 0012) : 6 événements câblés dans les services via `transaction.on_commit` (invitation, acceptation, lien à confirmer, demande envoyée, paiement reçu, reçu émis) — module `apps/common/notifications.py` (templates = point d'extraction i18n). Règles de contenu actées : JAMAIS d'info médicale ni de nom de centre dans un SMS patient, montant vers le tuteur seul, événement « lien à confirmer » anonyme. SMS uniquement vers les liens ACTIFS au moment de l'envoi ; partage tardif (après envoi) notifié. Invitation throttlée (5/jour par patient, 3/jour par téléphone cible).
  - **Purges planifiées** (Celery beat horaire) : `accounts.purge_expired_otp_codes` (grâce 24 h = fenêtre d'investigation d'abus) et `trustbridge.cancel_stale_intents` (`PSP_INTENT_STALE_HOURS`, défaut 24 ; webhook tardif sur intent annulé → 400 + AuditLog `payment.webhook_refused` pour la réconciliation).
  - **Trous d'API comblés** : `GET /centers/{c}/patients/{pk}/guardian-links/` (routage du partage au guichet — BILLING, liens actifs seuls, téléphone masqué non moissonnable), `POST /guardian/invitations/{pk}/decline/` (refus définitif), `paid_at` stocké (posé atomiquement avec le ledger) et exposé aux 3 casquettes, annulation d'invitation patient = revoke (verrouillé par test).
  - **Troisième porte de `PENDING_CLAIMANT_CONFIRMATION`** (revue guardian) : la fusion qui transfère un utilisateur revendiqué vers une cible portant des liens actifs suspend TOUS ces liens (sur-confirmation assumée des liens déplacés). Les trois portes : revendication OTP, fusion de doublons, transfert d'utilisateur par fusion.
  - **1855 tests verts** (`pytest` depuis `backend/`, ~3 min), dont les campagnes adversariales conservées en régression (`test_hardening.py`, `test_adversarial_api_recheck.py`, `test_adversarial_scheduling_wave1.py`, `test_adversarial_cash_wave2.py`, `test_adversarial_stats_wave2b.py`, `test_adversarial_s1.py`, `test_adversarial_s2.py`, `test_adversarial_s3.py`, `test_adversarial_s4.py`, `test_adversarial_s5.py`). ADR 0001→0018 dans `docs/adr/` (+ addenda S1 dans 0008/0015, S2 dans 0004/0008/0013, addendum d'implémentation S3 dans 0016, addenda LOT 1/2/3 + revue guardian dans 0017, addendum S4 dans 0011).
  - **Garde anti-double-débit** (revue guardian) : `create_payment_intent` refuse un nouvel intent tant qu'un intent `cree`/`en_cours` < `PSP_INTENT_GUARD_MINUTES` (défaut 15, borne basse refusée au boot) existe sur la même demande ; `pay/` concurrents sérialisés par le verrou de ligne (test 2 threads réels) ; admin des intents append-only. Exigences du futur `psp/stripe.py` actées dans l'addendum ADR 0009 (annuler l'intent périmé côté PSP, appel HTTP hors transaction, purge Celery des intents zombies).
  - **Chantier « Gâter les centres » vague 1 (backend)** — ADR 0013 & 0014 :
    - _Rendez-vous & file du jour_ (`apps/scheduling`) : `Appointment` (praticien nullable = RDV « avec le centre », invariant praticien-du-centre dans `save()`), machine à états `prevu → arrive → honore` / `manque` / `annule` pilotée par services (transitions sérialisées `select_for_update`), file du jour = `GET /centers/{c}/appointments/?date=` en heure locale `Indian/Comoro`, chevauchements signalés `overlaps` NON bloquants, passé refusé (5 min), horizon de réservation borné 2 ans, honor auto via `create_encounter(appointment=…)` (même centre + même patient, jamais de saut d'état, rollback complet). `reason` = champ OPÉRATIONNEL visible de tout le staff (distinct du clinique role-gaté — frontière documentée ADR 0013) ; pas d'AuditLog (RDV = exploitation non sensible). Rappels J-1 : beat `scheduling.send_appointment_reminders` (crontab 18h00 `CELERY_TIMEZONE`), SMS conforme ADR 0012 (ni motif, ni praticien, ni nom de centre), anti-doublon `reminder_sent_at` remis à NULL au déplacement. La fusion de doublons ré-ancre les RDV sur la cible (correctif guardian — sinon rappel vers le téléphone du doublon).
    - _Uploads durcis_ (`apps/common/uploads.py`, ADR 0014) : JPEG/PNG/WebP SEULS (jamais SVG), taille ≤ 2 Mo avant parsing, dimensions ≤ 2048² lues dans l'en-tête (anti-bombe), format RÉEL décodé par Pillow (extension/Content-Type client ignorés), ré-encodage complet strippant EXIF/GPS/ICC/XMP, nom stocké = uuid. Logo centre `POST|DELETE /centers/{pk}/logo/` (directeur seul, audité `center.updated`) exposé dans `HealthCenterSerializer` + `_CenterLabelSerializer` de `/auth/me/` ; avatar `POST|DELETE /auth/me/avatar/` (soi-même seul) exposé dans `MeSerializer` + annuaire staff UNIQUEMENT — jamais dans les vues croisées patient/tuteur. Remplacement sans orphelin (fichier précédent effacé du disque). Media servis par Django en DEBUG seulement.
    - _Rôles enrichis_ : `PATCH /centers/{c}/staff/{pk}/` (directeur seul, audité `staff.membership_updated`) — rôle d'un membership actif + identité SEULEMENT si compte « jamais revendiqué » (téléphone non vérifié ET mot de passe inutilisable) ; garde « dernier directeur » partagée rétrogradation/désactivation, verrouillée contre la course (`FOR UPDATE` ordonné — correctif guardian). `PATCH /auth/me/` : first/last name SEULS (phone/username ignorés, testé).
  - **Chantier « Gâter les centres » vague 2 (backend)** — ADR 0015 (+ addendum 2b) :
    - _Caisse : encaissement généralisé_ — **l'encaissement est le concept général, le paiement diaspora en est un cas particulier recouvert** (cadrage PO). Modèles `CashPayment` (méthode `especes` | `mobile_money` (opérateur `huri|mvola|autre`) | `pont_confiance`, FK ledger obligatoire), `CashPaymentReversal` (contre-passation OneToOne, motif obligatoire jamais audité en payload), `CashReceipt` (série « G- » séparée du reçu diaspora, race-safe, réconciliée avec le ledger) — les trois append-only ORM **et** triggers PostgreSQL. Écritures : `DEBIT caisse_especes|mobile_money / CREDIT creances_patient` via `LedgerTransaction.record()` seul ; solde = `invoice_balance_kmf()` (total − encaissements non contre-passés) ; paiement partiel par tranches, jamais au-delà du solde (verrou de ligne ; le webhook PSP re-vérifie le solde sous verrou → refus `payment.webhook_refused`) ; `register_payment_success` crée l'encaissement `pont_confiance` adossé à la transaction diaspora EXISTANTE (jamais deux écritures pour le même franc) ; devis/intent diaspora = solde restant ; garde miroir (intent récent bloque le guichet) ; facture `payee` → `emise` si contre-passation rouvre le solde ; contre-passer l'encaissement `pont_confiance` au guichet = IMPOSSIBLE (litige). API : `POST|GET /centers/{c}/invoices/{pk}/payments/`, `POST .../payments/{pk}/reverse/`, `GET /centers/{c}/cash-journal/?date=`, `GET /patients/me/cash-receipts/` (patient seul — le tuteur ne voit JAMAIS un reçu guichet). Pas de SMS guichet. Revue guardian : facture `annulee` fermée sur le rail diaspora (faille élevée corrigée), courses webhook/caissier/contre-passation écartées par threads réels.
    - _Pilotage_ (lecture seule, `apps/centers/stats_views.py`) : `GET /centers/{c}/stats/activity/` (tout staff — séries journalières RDV/consultations/nouveaux patients, taux de présence, répartition par praticien) et `GET /centers/{c}/stats/finances/` (BILLING seuls — recettes par jour/méthode, contre-passations à part jamais soustraites en silence, facturé vs encaissé, impayés) ; `GET /centers/{c}/invoices/unpaid/` (BILLING, téléphone masqué, tri borné). Fenêtre 30 j défaut / 366 max, bornes heure Comores, agrégats SQL purs (`assertNumQueries` 6/6/4), `unpaid_invoices_qs` = miroir ensembliste de `invoice_balance_kmf()` (accord verrouillé par test). Revue guardian : zéro faille, 21 probes pérennes.
    - _Intégralité KMF_ : `apps/common/money.py` (`validate_kmf_integral`) sur `TariffItem.price_kmf` (champ + `save()`) — un tarif fractionnaire rendrait la facture insoldable au guichet.
  - **Sprint S1 « Assainissement »** (plan SaaS §G de l'audit) — addenda ADR 0008 & 0015 :
    - _Permissions durcies_ : `IsGuardianWithScope` vérifie RÉELLEMENT la portée (403 sans lien actif porteur du scope — corps byte-identique jamais-lié/en-attente/révoqué, pas d'oracle ; `IsGuardian` = porte de l'espace sans scope pour profil/invitations) + garde-fou structurel testé sur toutes les routes `/guardian/` ; rôles source unique `apps/common/roles.py` (`BILLING_ROLES`, `CLINICAL_ROLES`) ; norme des refus actée : référence en URL → 404, référence dans le CORPS → 400 explicite (messages étranger/inexistant byte-identiques) ; throttles (user global 600/min généreux, `uploads` 20/h strict, webhook PSP et health EXEMPTÉS sur tous les chemins) ; Swagger `IsAdminUser` hors DEBUG. Arbitrages C.3 actés RÉVERSIBLES : litiges lecture + fusion de patients → BILLING ; `confirm-care`, tarifs, factures/demandes lecture tout staff inchangés.
    - _Cycles de vie_ : `POST invoices/{pk}/cancel/` (BILLING, motif obligatoire — `cancel_reason` visible BILLING SEUL, serializer par rôle ; conditions strictes ; demande `envoyee` laissée vivante mais rail fermé ; actes libérés refacturables), `POST encounters/{pk}/close/` (cliniques ; terminée = plus d'ordonnances ni carnet, facturation possible), `POST staff/{pk}/reactivate/` (directeur, audité).
    - _Confort API_ : annuaire `GET /centers/{c}/practitioners/` (tout staff, cliniques actifs, tableau nu), filtres (`encounters?patient=&date=&practitioner=`, `invoices?patient=&status=`, `payment-requests?status=`, `disputes?status=`), plage RDV `?from=&to=` (max 62 j).
    - _Vigilances 2a soldées_ : contrainte DB `tariff_price_kmf_integral` (migration à garde qui REFUSE l'existant fractionnaire), idempotence guichet (`idempotency_key` unique par centre, rejeu identique → 200 même reçu, référence incluse dans la comparaison, courses testées à threads réels).
    - _Correctifs guardian S1_ (4, verrouillés en régression) : hiérarchie de verrous unifiée **intent → demande → facture → centre** (l'annulation ne peut plus croiser un `pay/` — plus de débit réel vers un refus) ; double facturation concurrente des mêmes actes fermée (verrou consultation) ; rejeu d'idempotence à référence différente → 400 ; `cancel_reason` retiré des lecteurs non-BILLING. **Toute nouvelle écriture caisse doit respecter cette hiérarchie de verrous.**
    - _Frontend S1_ : litiges/fusion/journal/impayés gatés BILLING (sidebar `NAV_ROLE_GATES` + palette ⌘K + écrans auto-gardés), 403 tuteur = état vide bienveillant (« Aucun protégé pour le moment »), sélecteur praticien branché sur l'annuaire réel, grille calendrier en plage complète (plafond 200 RDV documenté, repli compteurs honnête), gestes annuler-facture/clôturer-consultation/réactiver-membre, clé d'idempotence auto sur l'encaissement (conservée sur échec, régénérée au succès), filtres factures/consultations/historique patient serveur.
  - **Sprint S2 « Casquettes & parcours »** — addenda ADR 0004/0008/0013 :
    - _RDV côté patient_ : `GET /patients/me/appointments/` (transversal, payload patient SANS `reason` ni username ombre, `?upcoming=`) + `POST .../{pk}/cancel/` (`prevu` seul, sous verrou — la course avec le check-in guichet est sérialisée). Self-booking hors périmètre (documenté).
    - _Consentement clinique porte C_ : `POST|DELETE /centers/{c}/patients/{pk}/consents/clinical/` (BILLING, `{guardian_link, collected_via ∈ papier|oral}`, patient NON revendiqué seulement — revendiqué → 400 « gère lui-même ») ; `Consent.collected_by/collected_via` (migration, historique intact) ; audité `consent.granted_by_center`/`revoked_by_center` références-only. **Invariant des trois portes vérifié par test + course réelle** : le consentement guichet meurt à toute revendication/fusion, la confirmation du lien ne restaure QUE `paiements`. Correctif guardian S2 (élevé) : TOCTOU revendication×octroi fermé par relecture patient+lien sous `select_for_update` (ordre patient → lien, comme `claim_profile`).
    - _Tuteur_ : `GET /guardian/links/` (`IsGuardian`, tous statuts — « en attente de confirmation » enfin visible ; payload strict sans téléphone/scopes/claim_status) ; `PATCH /guardian/profile/` (pays ISO-2, devise préférée — inerte dans les devis, vérifié).
    - _Frontend S2_ : « Mon profil » ADAPTÉ des écrans Vireo `Profile`/`ProfileSettings` (routes `/centre/profil[/parametres]`, modale supprimée, casquettes par centre, barre d'enregistrement collante) ; **multi-rôles cumulés** (`CenterContext.roles`, toutes les gardes basculées — le cumul élargit, ne restreint jamais) ; `/patient/rendez-vous` (annulation bienveillante, statuts lecteur-patient dégenrés, « Manqué » jamais rouge) ; protégés tuteur en trois groupes (« à son rythme », anciens liens repliés) ; consentement guichet modalisé (texte de responsabilité, retrait à égalité visuelle) ; RDV `arrive` → « Créer la consultation » (patient verrouillé, honor auto). Revue UX care passée (7 correctifs).
  - **Sprint S3 « Dossier patient enrichi »** — ADR 0016 (+ addendum d'implémentation) :
    - _Cadrage_ : deux décisions cadre — **aucune exposition tuteur en S3** (toute lecture clinique tuteur est conditionnée à la garde SV.1.1 non implémentée ; serializer tuteur gelé par assertion négative sur les 12 nouveaux champs) et **re-segmentation clinique/administrative** (groupe sanguin/taille/poids = données de santé RGPD art. 9, sphère clinique malgré la liste F.2.1 du PO). PDF différé (photos JPEG/PNG/WebP seules, socle ADR 0014 tel quel) ; **directives anticipées HORS périmètre** (cadrage dédié à ouvrir, candidat SV/S6).
    - _Identité élargie_ : `address`, `phone_alt`, `national_id`, `emergency_contact_name/phone/relationship` sur `PatientProfile` (téléphones E.164, tous dans `IDENTITY_FIELDS` — R-API-2 intact) ; détection de doublons porte C `GET /centers/{c}/patients/similar/` (non bloquante, périmètre centre, aucun oracle — numéro étranger et numéro fantôme byte-identiques).
    - _Fiche médicale_ : `PatientMedicalFile` (OneToOne, groupe sanguin à choix fermés + notes, `get_or_create` au premier write, forme vide constante en GET) cliniques seuls + patient ; `HealthRecordEntry.entry_type` étendu (`chirurgie`, `antecedent_familial`, `observation`).
    - _Signes vitaux en visite_ : `VitalSigns` (FK encounter obligatoire — l'hospitalisé = S6 ; `measured_by` invariant praticien-du-centre ; au moins une mesure ; bornes de plausibilité `VITAL_SIGNS_BOUNDS` + diastolique < systolique, jamais stocké hors bornes ; consultation `terminee` refusée) ; lecture patient transversale `GET /patients/me/vital-signs/`.
    - _Documents en pièces jointes_ : `PatientDocument` + **`PrivateMediaStorage`** (`PRIVATE_MEDIA_ROOT`, `url()` lève TOUJOURS, `/media/` → 404 testé — un document médical n'est jamais servi statiquement) ; téléchargement uniquement par endpoints authentifiés (`FileResponse` attachment, nom neutre `document-{id}.ext`, nosniff ; `CORS_EXPOSE_HEADERS` Content-Disposition) ; archivage définitif (jamais de suppression) ; bornage centre producteur (un document reste attachable à une consultation terminée — les résultats arrivent après la visite) ; throttle `uploads` ; en prod : `X-Accel-Redirect` + racine hors document-root (check-list SV.2).
    - _Assurance/mutuelle_ : `PatientInsurance` transversale (lecture tout staff du périmètre + patient, écriture BILLING) ; tiers payant hors périmètre (chantier facturation ultérieur).
    - _Transverses_ : `merge_profiles` étendu (ré-ancrage documents/assurances, fiche du doublon reprise SEULEMENT si la cible n'en a pas, signes vitaux suivent leurs encounters) ; 6 actions AuditLog références-only (jamais une valeur mesurée, un titre, un assureur) ; seed démo enrichi ; contrat frontend à jour.
    - _Guardian S3_ : **zéro faille confirmée** (la passe la plus propre du dépôt), 19 probes pérennes ; 6 vigilances non bloquantes consignées en SV.2 (triggers DB absents sur les 4 nouvelles tables, admins sans `readonly_fields`, écriture d'assurance transversale à re-trancher au tiers-payant, `measured_at` libre, visibilité document post-fusion, câblage media privé au déploiement).
    - _Frontend S3_ : fiche patient en 6 cartes (Fiche médicale et Documents **jamais montées** hors rôles cliniques, fetchs compris) ; bandeau doublons non bloquant à la création (debounce, `role="status"`) ; `VitalSignsCard` (unités dans les labels, virgule décimale acceptée, erreurs de bornes champ par champ) ; upload dropzone Vireo + téléchargement authentifié `apiDownload` (blob, jamais d'URL statique ni d'`<img src>`) ; carnet patient 4 onglets (Documents + « Ma santé » : fiche, mesures en langage simple — « Tension 120/80 », « Oxygène 96 % ») ; profil patient enrichi (personne à prévenir, assurances lecture seule). UX care passée (7 correctifs — dont erreurs de téléchargement dans la carte concernée, échec des mesures jamais muet, grilles de formulaire en `auto-fit`).
  - **Sprint S4 « Le tenant de plein droit »** — ADR 0017 (+ addenda LOT 1/2/3 et addendum de revue guardian ; addendum S4 dans ADR 0011) :
    - _Arbitrages PO (14/08/2026)_ : (1) **suspendre un centre ferme le rail diaspora, et lui seul** — le soin et la caisse locale continuent, suspendre ne doit jamais empêcher de soigner ni renvoyer un centre au papier (sanction commerciale = S5) ; (2) back-office = API cloisonnée + 4ᵉ espace sobre ; (3) RGPD = demande depuis l'espace + traitement par l'exploitant. Arbitrage complémentaire : **le paiement en vol atterrit** (webhook NON gardé par le KYC — un intent n'existe que si le centre était actif à son ouverture, donc la carte est déjà débitée ; refuser laisserait un débit sans reçu. **Encaisser n'est pas verser** : retenir les fonds d'un centre suspendu est le travail du reversement, non construit).
    - _4ᵉ casquette_ : `accounts.PlatformStaff` (OneToOne User, `support`/`admin`, `is_active`) + `IsPlatformStaff` (marqueur `platform_gate`) — **`is_staff`/`is_superuser` ne donnent AUCUN droit d'API** (un superuser sans ligne active = 403 partout, testé) ; `platform_staff` dans `/auth/me/` = routeur du 4ᵉ espace ; garde-fou structurel sur l'URLconf `/platform/` (vérifie aussi qu'aucune vue n'hérite en silence d'`IsAuthenticated`). **Invariant du sprint** : l'exploitant ne voit AUCUNE donnée clinique ni PII patient (tests de champs négatifs, y compris sur la file RGPD dont l'objet est une personne : id de compte + casquettes en booléens, jamais un nom).
    - _Onboarding du tenant_ : `POST /platform/centers/` (centre + premier directeur en UNE transaction, compte ombre → OTP, jamais de mot de passe transmis), `/platform/centers/similar/` (non bloquant, miroir de la porte C), `POST .../directors/` (amorçage de secours).
    - _KYC à effets réels_ : `centers.KycDocument` (stockage privé, socle ADR 0014, archivage définitif) déposé par le directeur, lu par la plateforme ; `set_center_kyc_status()` sous verrou (motif OBLIGATOIRE en suspension, `kyc_reason` visible du directeur du centre SEUL, jamais en payload d'audit) ; `HealthCenterAdmin` read-only sur le KYC = l'API plateforme est la porte unique ; garde `_require_center_can_collect` remontée en amont (création, partage, envoi, intent — porte patient comprise) avec messages distincts `en_attente` vs `suspendu`.
    - _Admin Django refermé_ : mixins partagés `apps/common/admin.py` ; `medical`/`patients`/`Invoice`/`PaymentRequest`/`Dispute`/**`ConsentAdmin`** en lecture seule stricte (dettes SV.2 soldées) ; `tests/test_admin_hardening.py` avec **fermeture du registre** (un modèle sensible nouveau non déclaré casse la suite).
    - _Journal du directeur_ : `AuditLog.center` (FK nullable, **aucun rétro-remplissage** — append-only) ; `GET /centers/{c}/audit-log/` directeur SEUL avec **liste blanche** (exploitation oui ; clinique et consentements JAMAIS — un scénario clinique complet produit un journal vide, testé) ; `journal_starts_at` honnête ; `?action=` sans oracle.
    - _Réconciliation PSP_ : `GET /platform/reconciliation/` (6 codes d'incident distincts, liste blanche de refs testée contre un payload pollué).
    - _RGPD (ADR 0007 implémenté)_ : `ErasureRequest` (une demande ouverte) + `User.anonymized_at` ; `anonymize_user()` (jamais de DELETE, pivot téléphone à NULL, liens de tutelle révoqués en cascade, **carnet médical conservé** — classe de tests dédiée pour que ce soit lisible comme une décision) ; gardes de refus (dernier directeur, intent PSP en cours, dernier admin plateforme) ; `GET /auth/me/export/` (portabilité art. 20, rejoue les règles de visibilité des écrans — **le verrou tuteur S3 tient**, throttle `data_export`) ; motif de refus **rendu à la personne** (art. 12.4) ; une demande bloquée reste `en_attente`.
    - _Guardian backend S4_ : **6 failles corrigées** dont 2 élevées — (1) un exploitant `admin` s'amorçait directeur d'un centre et lisait tout le registre patients (l'invariant était appliqué aux payloads, jamais à la personne de l'exploitant ; garde posée sur les 2 portes, réserve honnête : un second numéro reste amorçable) ; (2) un directeur cassait la file RGPD de toute la plateforme en 500 (`select_for_update` hors transaction sur un GET — invisible des tests, `django_db` enveloppant tout) ; + course des 2 derniers admins plateforme, TOCTOU paiement en vol × anonymisation (**hiérarchie de verrous étendue : utilisateur → intent → demande → facture → centre**), `anonymize_user` sans gardes propres, garde KYC faisant confiance à l'objet mémoire. 42 probes pérennes (`test_adversarial_s4.py`).
    - _Frontend S4_ : 4ᵉ espace `(plateforme)` sobre (chrome dédié sans ⌘K ni customizer, **second manifeste** de navigation, `CenterContext` jamais monté) — centres, détail + transitions KYC modalisées, réconciliation, file RGPD (action non montée tant qu'un blocker existe) ; côté centre : pièces KYC (directeur), journal d'audit (`/centre/journal`), bandeau/carte KYC disant **ce qui continue avant ce qui est fermé** ; grand public + personnel : carte « Mes données » (export + demande d'effacement, deux registres de phrases, `ERASURE_COPY`). Revues : UX care (11 correctifs — dont le **piège de focus de la Modal grand public** qui donnait le focus au bouton d'action : une touche Entrée déposait une demande d'effacement ; « vos proches ne pourront plus payer vos soins » ; l'absence de rétractation enfin dite) et guardian frontend (4 constats — modales d'écriture quittables **en vol** sur un geste irréversible → prop `busy` ; **rendu des payloads d'audit passé en liste blanche fail-closed** : une clé ajoutée demain côté backend ne s'affiche plus toute seule).
  - **Sprint S5 « Abonnement & Support »** — ADR 0018 (+ addenda LOT 1/2/3 et addendum de revue guardian) :
    - _Arbitrages PO (14/08/2026)_ : (1) **factures SaaS dans l'app, règlement hors ligne** (le paiement en ligne du centre dépend des rails comoriens — risque n° 1 non instruit) ; (2) **grâce puis gel de l'administratif, soin et caisse intacts** ; (3) **quotas indicatifs, jamais bloquants**.
    - _Registre SÉPARÉ_ (`apps/billing`) : aucune écriture dans le ledger des soins — un cycle d'abonnement est mutable alors que le ledger est append-only à 3 niveaux ; le fléchage du ledger est patient-centré ; ses lecteurs sont les gens du centre (une facture SaaS dans `stats/finances` serait un bug de lecture) ; précédent `CashReceipt` vs `Receipt`. **Étanchéité testée dans les DEUX sens.** Immuabilité empruntée là où elle a du sens : `SubscriptionPayment` append-only, corrigé par contre-passation motivée.
    - _État d'abonnement = axe INDÉPENDANT du KYC_ (`essai|actif|impaye|suspendu|resilie`) + **garde distincte** `require_center_can_administer` (relit le statut en base ; `_require_center_can_collect` n'est PAS touchée — **le rail diaspora reste OUVERT sur un impayé** : le fermer punirait le tuteur et le patient, le KYC porte déjà ce levier). `impaye` ne ferme RIEN ; seul `suspendu` gèle (personnel, tarifs, stats) ; `resilie` garantit lecture et export — **jamais de prise d'otage des données**. `deactivate_staff_member` volontairement NON gelé (sécurité + `anonymize_user` l'appelle).
    - _Facturation SaaS_ : `SubscriptionInvoice` (série « A- » **globale Chioni** via ligne compteur verrouillée — pas de `SEQUENCE` (non transactionnelle : un rollback brûlerait un numéro), pas de verrou sur `HealthCenter` (contention avec `Receipt.issue()`)), montant figé + snapshot d'offre, règlements partiels jamais au-delà du solde, contre-passation motivée. Beat quotidien d'émission (périodes mensuelles par tenant), `actif → impaye` automatique, **`suspendu` JAMAIS automatique** (un virement non encore saisi ne doit pas geler un centre la nuit). Relances SMS **J+0/J+7/J+21 puis silence**, au directeur seul, solde restant, sans même le nom du centre.
    - _Module Support_ (`apps/support`) : `SupportTicket` + `SupportMessage` append-only + pièces jointes (socle ADR 0014, stockage privé), ouverture par **tout staff actif**, lecture auteur + directeur, `author_side` posé par le service. **Le gel ne s'y applique PAS** (verrouillé 3× : test « ce qui continue », test de contraste, sonde structurelle). Risque résiduel **nommé** : aucun champ ne pointe un patient, contenu jamais en payload d'audit, avertissement au moment d'écrire — on informe, on ne prétend pas empêcher.
    - _Admin Django totalement refermé_ : `PlatformStaffAdmin` et `StaffMembershipAdmin` en lecture seule (dette ADR 0017 soldée) ; API `/platform/operators/` (`admin` seul, même en lecture) + commande `create_platform_staff` pour le bootstrap ; garde miroir `_refuse_center_staff_as_operator`.
    - _Guardian backend S5_ : **4 failles corrigées** dont 1 élevée — S4 avait fermé la porte d'entrée, S5 laissait la sortie ouverte : un exploitant `admin` se désactivait, s'amorçait directeur, se réactivait (**même compte, 3 appels**) et lisait le registre patients ; + les 2 gardes miroir passaient sous course réelle (verrou sur la ligne `User`, pivot), relance SaaS envoyée **2×** (cadence dépassable), deadlock sur 2 rétrogradations. 45 probes (`test_adversarial_s5.py`). **Invariant « le gel n'empêche jamais de soigner » désormais garanti par construction** : 2 sondes fail-closed ferment la liste des modules qui importent la garde et des fonctions qui l'appellent.
    - _Frontend S5_ : « Mon abonnement » (directeur, auto-gardé avant fetch) + bandeaux disant **ce qui continue avant ce qui est fermé**, Support (tout staff, avertissement de confidentialité au contact du champ) ; 8 écrans plateforme (contrats, factures, offres, exploitants, tickets) ; **`DocumentPaper`** = module invoice de Vireo adapté, **paramétré par l'émetteur** (Chioni ou le centre, logo compris) et prêt pour les factures patient et reçus « R- »/« G- ». Revues : UX care (18 correctifs — l'avertissement pointait vers un « numéro de dossier » **inexistant à l'écran** ; la facture s'imprimait **blanc sur blanc** en thème sombre ; une touche Entrée **enregistrait un règlement**) et guardian frontend (dérive de contrat : les 4 actions support n'avaient jamais atteint le frontend — **le fail-closed protège la donnée mais dégrade en silence**, remède SV : test de parité).
  - **Outils dev** (DEBUG obligatoire) : `python manage.py simulate_psp_payment [--latest] [--fail]` rejoue le webhook PSP signé ; `python manage.py seed_demo` (idempotent, services uniquement) peuple la base de démo — Clinique Ylang KYC actif + staff (`directeur.demo`, `medecin.demo`, `secretaire.demo`, `caissier.demo`), patiente revendiquée `patient.demo` (+2693440001), `tuteur.demo` (lien actif + demande 20 000 KMF « à payer »), `tuteur2.demo` (lien en attente de confirmation du titulaire, obtenu par le VRAI flux porte A → revendication), mot de passe commun `ChioniDemo!2026`. En dev le code OTP s'affiche dans la console du runserver (logger `chioni.sms`, config LOGGING dans les settings).
- **Frontend construit et branché — les 4 espaces sont démontrables** (centre, patient, tuteur, plateforme ; `npm run build` vert, 58 routes, First Load ~101–140 kB). Élagage Vireo fait (~49 % supprimé ; tout reste repêchable dans `vireo template/`). Architecture : ADR 0011 ; contrat API condensé pour le frontend : `docs/frontend/api-contract.md`.
  - **Chantier « Gâter les centres » vague 3 (frontend centre)** : `/centre/rendez-vous` (deux modes — file du jour par défaut : pointage un-clic avec patch de ligne sur place, confirmation SEULEMENT sur « Manqué »/« Annuler » ; grille Calendrier adaptée de l'app Calendar de Vireo, code-splittée, compteurs mensuels via `stats/activity` + détail sur la semaine sélectionnée, l'UI le dit honnêtement), `/centre/caisse` (journal du jour, totaux par méthode, contre-passations signalées), `/centre/impayes` (tri serveur, téléphone masqué du backend tel quel), section Caisse sur la facture (encaissement pré-rempli avec le solde = 1 clic, contre-passation modalisée à motif obligatoire, `pont_confiance` non contre-passable), dashboard piloté par les vraies stats (bloc finances jamais monté hors BILLING), logo centre (Settings + sidebar), avatar + nom (`ProfileDialog`), rôles/identité dans Personnel, onglet patient « Reçus de caisse » (vocabulaire simple, « Annulé » jamais « contre-passé »). Revue UX care passée (pointage sans rechargement, piège de focus des modales du socle, navigation clavier sidebar réparée, ton apaisant côté patient).
  - **Trous de contrat listés par la vague 3 (à combler côté backend au besoin)** : endpoint plage RDV `?from=&to=` (rendrait le mois complet en 1 appel), annuaire « praticiens actifs » lisible par tout staff (les sélecteurs se rabattent sur `encounters_by_practitioner` 12 mois), `received_by`/`reversed_by` ids bruts sans résolution de nom pour non-directeur, item `reversal` du journal sans id de facture (lien de réconciliation impossible), `StaffUser` n'expose pas l'état d'activation (griser l'identité proactivement).
  - **Socle** : client API `src/lib/api.ts` (Bearer, refresh single-flight — le refresh SimpleJWT est à usage unique, normalisation des 3 formats d'erreurs DRF, `Retry-After`), types/endpoints par domaine (`src/lib/types.ts`, `src/lib/endpoints/`), libellés FR centralisés `src/lib/labels.ts` (enums, `formatKmf/Eur`, `formatWait`, `maskPhone` — point d'extraction i18n unique), `AuthContext` (+ déconnexion propagée entre onglets) et `CenterContext` (centre actif multi-centres), gardes client `RequireSpace` par layout d'espace.
  - **Routes** : `(bare)` landing `/` + `/auth/sign-in|verify|staff` (OTP 6 cases avec autofill SMS, staff par mot de passe) + `/espaces` (choix d'espace / orientation portes B et tuteur) ; `(centre)/centre/*` sous le shell Vireo complet (dashboard honnête sans données inventées, patients porte C + fusion, consultations role-aware, factures→demandes→clôture avec reçu, litiges, tarifs, personnel, paramètres KYC) ; `(patient)/patient/*` et `(tuteur)/tuteur/*` sous chrome léger mobile-first `shell-lite` (tab bar basse, cibles ≥ 44 px).
  - **Revues passées** (guardian frontend + UX care, 41 correctifs appliqués) : contrastes AA via tokens `--ax-*-700`, carte de transparence tuteur (devis : KMF reçu / taux figé / frais explicites ; `pay/` à corps vide ; attente honnête + garde locale anti-re-paiement), porte de confirmation du titulaire en tête d'accueil patient (« Est-ce bien votre proche ? », refus définitif modalisé), accusé patient = « soin reçu » (jamais « vu »), reçu centre « dont frais » (jamais « + frais »).
- Git sur `main`, working tree propre — le chantier frontend 3 espaces ET la vague 1 backend « Gâter les centres » sont commités.
- **Auth OTP SMS en place** : `/api/v1/auth/otp/request|verify/` (code 6 chiffres haché HMAC, 10 min, 5 tentatives, anti-énumération byte-à-byte, throttles par téléphone + IP), abstraction SMS `apps/common/sms.py` (console/memory/stub, `stub` à implémenter pour agrégateurs comoriens ; garde-fou boot comme le PSP), activation des comptes ombres + revendication STRICTE de profil (jamais sur simple téléphone déclaratif d'un profil guichet — ADR 0010). Login mot de passe JWT conservé pour le staff/back-office. Envoi via Celery.
  - **Porte de confirmation du titulaire (invariant éthique — cœur du produit)** : toute arrivée d'un lien de tutelle sur un profil REVENDIQUÉ exige la confirmation explicite du patient — que le lien arrive par revendication OTP OU par fusion de doublons. Le lien passe en `PENDING_CLAIMANT_CONFIRMATION` (consentements révoqués, aucun accès `paiements`) jusqu'à ce que le patient l'active via `POST /patients/me/guardians/{link}/confirm/` (ou le refuse définitivement via `/decline/`). Tant qu'un profil est NON revendiqué, le lien porte-A reste actif (le tuteur gère le dossier — cas Mariama sans smartphone). Ne jamais réintroduire un lien tuteur `ACTIVE` sur un profil revendiqué sans passer par cette porte.
- **Reste à faire — LA référence est [docs/audit-saas-multitenant.md](docs/audit-saas-multitenant.md)** (audit permissions × vues + périmètre fonctionnel, remarques PO assimilées, **plan des sprints S1→S10 jusqu'à la complétion du SaaS** : ~~S1 assainissement~~ **FAIT le 13/08/2026**, ~~S2 casquettes/parcours~~ **FAIT le 14/08/2026** (reliquats S1 absorbés ; guardian + UX care passées ; reliquats S2 : état persistant du consentement clinique non lisible côté centre — endpoint de lecture à prévoir si le PO veut l'afficher au chargement ; titre du chrome lite à surcharger par page ; modales centre en grille figée `1fr 1fr` à assouplir), ~~S3 dossier patient enrichi~~ **FAIT le 14/08/2026** (guardian zéro faille + UX care passées ; reliquats S3 : identité élargie absente du modal de création porte C — backend prêt, ajout rapide si le PO le veut ; `source_encounter` non proposé à l'upload de documents ; fiche santé patient en échec de chargement silencieux assumé ; directives anticipées = cadrage dédié à ouvrir ; PDF différé — arbitrage réversible ADR 0016 ; lecture clinique tuteur toujours fermée, conditionnée à SV.1.1), ~~S4 tenant de plein droit + RGPD~~ **FAIT le 14/08/2026** (guardian backend : 6 failles corrigées dont 2 élevées ; guardian frontend : 4 constats corrigés, aucun élevé ; UX care ×2 ; reliquats S4 : miroir centre « paiement diaspora refusé » non construit ; pas d'export CSV du journal ni de la réconciliation ; self-service d'inscription d'un centre = S5 ; pièces KYC en photos seulement — PDF différé, arbitrage réversible commun avec l'ADR 0016 ; `PlatformStaffAdmin` encore écrivable ; token `--ax-text-subtle` à 4,32:1 sous AA, arbitrage de design system à trancher), ~~S5 abonnement & support~~ **FAIT le 15/08/2026** (guardian backend : 4 failles corrigées dont 1 élevée ; guardian frontend : 1 constat moyen ; UX care : 18 correctifs ; reliquats S5 : paiement en ligne du centre hors périmètre — rails locaux non instruits ; support patient/tuteur non ouvert ; pas de notification quand Chioni répond ; `/platform/centers/` n'expose pas le logo ; « Émettre la période due » sans modale de confirmation ; token `--ax-text-subtle` toujours sous AA), S6 hospitalisation, S7 HRM, S8 équipements, S9 pharmacie hors-périmètre — pivot acté : la pharmacie devient un acteur hors tenant, demandes de disponibilité anonymisées par conception —, S10 CRM santé/comptabilité ; + chantiers à clés parallèles : Stripe réel `psp/stripe.py` (addendum ADR 0009), SMS agrégateur comorien (ADR 0012 : retry borné, dead-letter, art. 14), i18n shikomori, PWA/offline + mode dégradé caisse, étude paiement partenaire local (risque n° 1), étude IA de reprise du papier). Le test manuel de bout en bout des 3 espaces (dont parcours centre « Gâter les centres ») a été **validé par le PO le 13/08/2026**. Limitation assumée pour mémoire : ré-établissement d'un lien de tutelle révoqué impossible (l'UI le dit honnêtement). Vigilances actées par le guardian : ne jamais exposer `Dispute.reason` à l'autre partie si des vues litige patient/tuteur apparaissent ; règle « pas de PII dans les payloads d'audit » = contrat de revue permanent ; jamais de HTML dynamique côté frontend (tokens en localStorage) + CSP à poser au déploiement ; le contenu d'un SMS suit la visibilité dans l'app, jamais l'inverse. Vigilances vague 1 (guardian) : identité d'un compte ombre partagé entre tenants éditable par chaque directeur (à trancher si le cas apparaît) ; pas de throttle dédié sur les uploads (CPU Pillow — à couvrir avec un throttle user global) ; `/media/` en prod = front web server + `nosniff` (contrat ADR 0014) ; RDV annulé/déplacé après le rappel J-1 → pas de SMS correctif (limitation produit assumée, ADR 0013). Vigilances vague 2 (guardian) : garde d'intégralité tarif app-level seulement (contrainte DB `price_kmf = ROUND(price_kmf)` + audit des lignes fractionnaires à poser au déploiement) ; pas de jeton d'idempotence sur l'encaissement guichet (double-clic = deux tranches réversibles) ; `CashPayment` non fenêtré comme `LedgerEntry` (contrat de revue ADR 0015) ; `InvoiceAdmin.status` éditable = seule porte vers `annulee` ; un encaissement contre-passé disparaît rétroactivement de sa journée dans les séries (le champ `reversals` du jour de la contre-passation est la piste de rapprochement — à retraiter si un export comptable figé apparaît). **Arbitrages S2 TRANCHÉS PAR LE PO (14/08/2026 — à implémenter au sprint SV, voir audit §SV)** : (1) chaîne « le centre fabrique le tuteur » → **option (b)** : consentement guichet INTERDIT sur un lien initié par le centre lui-même (provenance du lien à tracer ; si une lecture clinique tuteur devait arriver avant SV, cette garde devient un pré-requis BLOQUANT de ce chantier-là) ; (2) mode `oral` → **supprimé** : papier signé obligatoire pour le consentement clinique guichet (trace opposable, éviter les litiges des consentements oraux). L'inventaire complet des exceptions non bloquantes à solder est consigné dans le **sprint SV (validation finale)** de l'audit — toute exception nouvelle des futures revues s'y ajoute. Vigilance : `ConsentAdmin` sans `readonly_fields` (durcissement avec S4 ou SV).

## Stack et conventions

### Backend (Django)

- Django 5 + Django REST Framework ; PostgreSQL ; Redis + Celery pour les tâches asynchrones (SMS, notifications, relances).
- Base de dev locale (déjà créée) — à consommer via `.env` (jamais de secrets en dur dans le code ; ces valeurs sont locales uniquement, les environnements déployés auront les leurs) :
  ```env
  DB_NAME=chioni_db
  DB_USER=postgres
  DB_PASSWORD=admin
  DB_HOST=localhost
  DB_PORT=5432
  ```
- Multi-tenant : les données d'exploitation (RDV, caisse, personnel) sont cloisonnées par centre (`HealthCenter`) ; le carnet de santé appartient au patient et est transversal, sous consentement ; l'argent vit dans un ledger en double entrée, append-only.
- Toute action sensible (argent, données médicales, consentements) écrit dans `AuditLog` (immuable).
- **Règle d'ingénierie (issue de la revue adversariale)** : les invariants de `GuardianLink`, `Invoice`, `PatientProfile` et `PaymentRequestShare` vivent dans `save()`/`clean()` — ne JAMAIS muter ces modèles par `update()`/`bulk_update()` bruts ; toute écriture passe par un service qui rejoue `save()`. Les transitions les plus sensibles ont en plus un trigger PostgreSQL (voir ADR 0006). Les probes résiduelles documentées dans `backend/tests/test_hardening.py` (machine à états PaymentRequest, cohérence intent↔ledger, `active_scopes` + `shared_with` à combiner dans les permissions) sont des invariants OBLIGATOIRES de la couche API/services.
- Auth : téléphone + OTP SMS en priorité ; un même `User` peut cumuler des rôles (médecin ET tuteur).
- Tests obligatoires sur : permissions/cloisonnement tenant, ledger, consentements.

### Frontend (Next.js / Vireo)

- **Politique front-web design (actée par le product owner)** : exploiter les assets du template Vireo **en les adaptant aux besoins de Chioni, jamais coder en dur** ce qui existe dans le template (icônes, écrans, apps, widgets, patterns). Avant tout composant neuf : repêcher dans `vireo template/` (lecture seule — on copie vers `frontend/`). Le module web app **Calendar** de Vireo est la base attendue du module rendez-vous.
- Toujours réutiliser les composants et tokens `--ax-*` de Vireo avant d'écrire du neuf. Écrans utiles déjà présents : dashboard `healthcare`, auth (`sign-in`, `two-step` → OTP), apps `calendar`/`contacts`/`chat`, tables, formulaires, profile, HRM etc.
- Trois espaces : centre/médical (dashboard riche), patient & tuteur (parcours simplifiés, mobile-first, littératie numérique faible), site vitrine (landing).
- Exigences UX non négociables : mobile-first sur Android d'entrée de gamme, pages légères (faible connectivité aux Comores), vocabulaire simple sans jargon, accessibilité (contrastes, tailles de touche).

### Principes transverses

- Secret médical : le tuteur qui paie ne voit PAS le dossier médical par défaut — uniquement demandes de paiement, montants, nature générique de l'acte, reçus. Tout accès élargi = consentement explicite, tracé, révocable.
- RGPD dès la conception (les tuteurs sont résidents UE) : minimisation, consentements, droit d'effacement.
- Le module paiement est une abstraction PSP (**Stripe retenu** côté diaspora) ; le ledger interne en double entrée fait foi. Conversion EUR→KMF transparente : taux affiché avant paiement, figé à la transaction, reçu en double devise, chaque écriture du ledger porte sa devise et le taux appliqué.
- Chantier dédié « IA de reprise du papier » (onboarding des centres par numérisation de leurs archives — étude à mener, voir §5.5 de l'étude des besoins) : l'IA propose, un soignant valide ; provenance tracée jusqu'au document source.

## Stack Claude du projet

### Agents projet (`.claude/agents/`)

- **chioni-django-architect** — conception et implémentation backend (modèles, DRF, multi-tenant, ledger, permissions).
- **chioni-vireo-frontend** — construction du frontend en exploitant le template Vireo.
- **chioni-health-data-guardian** — revue adversariale de tout code touchant l'argent, les données médicales ou les consentements. À lancer après chaque feature sensible.
- **chioni-ux-care** — revue « soin et attention » des parcours utilisateurs (simplicité, mobile-first, accessibilité, ton).

### Skills globaux à utiliser selon la tâche

| Tâche                                            | Skill(s)                                     |
| ------------------------------------------------ | -------------------------------------------- |
| Backend Django / DRF / ORM                       | `django-expert`, `django-patterns`           |
| Sécurité Django (auth, permissions, déploiement) | `django-security`                            |
| Next.js App Router, RSC, server actions          | `nextjs-developer`                           |
| Design UI/UX, palettes, typographie, guidelines  | `ui-ux-pro-max`, `frontend-design`           |
| Audit d'accessibilité / bonnes pratiques web     | `web-design-guidelines`                      |
| Graphiques et dashboards                         | `dataviz`                                    |
| Revue de code / sécurité avant merge             | `code-review`, `security-review`, `simplify` |
