# Chioni backend — environment template (copy to .env and fill in real values)
SECRET_KEY=change-me
DEBUG=False

# PostgreSQL
DB_NAME=chioni_db
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Redis (Celery broker / result backend)
REDIS_URL=redis://localhost:6379/0
