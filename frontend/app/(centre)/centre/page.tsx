/*
 * Chioni — /centre → centre overview dashboard.
 * Currently the Vireo Healthcare dashboard as-is (demo data); it will be
 * wired to the real center activity in a later phase.
 */
export { Healthcare as default } from '@/screens/dashboards/Healthcare';
