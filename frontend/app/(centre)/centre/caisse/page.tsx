'use client';
/*
 * Chioni — /centre/caisse : le journal de caisse du jour (rôles BILLING).
 */
import { CashJournal } from '@/screens/centre/CashJournal';

export default function Page() {
  return <CashJournal />;
}
