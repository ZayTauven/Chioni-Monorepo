'use client';
/*
 * Chioni — /centre/hospitalisation : tableau d'occupation et séjours (S6).
 */
import { Inpatient } from '@/screens/centre/Inpatient';

export default function Page() {
  return <Inpatient />;
}
