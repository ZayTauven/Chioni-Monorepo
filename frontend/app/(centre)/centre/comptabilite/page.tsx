'use client';
/*
 * Chioni — /centre/comptabilite : les relevés de caisse figés (rôles BILLING).
 */
import { AccountingExports } from '@/screens/centre/AccountingExports';

export default function Page() {
  return <AccountingExports />;
}
