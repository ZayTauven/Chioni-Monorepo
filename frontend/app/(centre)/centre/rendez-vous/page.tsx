'use client';
/*
 * Chioni — /centre/rendez-vous : l'agenda et la file du jour de la secrétaire.
 */
import { Appointments } from '@/screens/centre/Appointments';

export default function Page() {
  return <Appointments />;
}
