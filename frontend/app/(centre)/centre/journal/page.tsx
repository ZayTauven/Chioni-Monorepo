/*
 * Chioni — /centre/journal : le journal d'audit du centre (directeur seul).
 */
import { AuditJournal } from '@/screens/centre/AuditJournal';

export default function Page() {
  return <AuditJournal />;
}
