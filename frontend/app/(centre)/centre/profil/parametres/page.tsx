/*
 * Chioni — /centre/profil/parametres : modification du profil (photo, nom).
 */
import { ProfileSettings } from '@/screens/centre/ProfileSettings';

export default function Page() {
  return <ProfileSettings />;
}
