/*
 * Chioni — /centre/abonnement : le contrat du centre avec Chioni (directeur seul).
 */
import { Subscription } from '@/screens/centre/Subscription';

export default function Page() {
  return <Subscription />;
}
