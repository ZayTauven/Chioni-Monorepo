'use client';
/*
 * Chioni — /centre/impayes : factures émises à solde restant (rôles BILLING).
 */
import { UnpaidInvoices } from '@/screens/centre/UnpaidInvoices';

export default function Page() {
  return <UnpaidInvoices />;
}
