'use client';
/*
 * Chioni — /centre/equipe : le planning collectif (S7, ADR 0020 décision 6).
 * Ouvert à TOUT membre actif du centre — c'est la seule lecture RH qui l'est.
 */
import { TeamDay } from '@/screens/centre/TeamDay';

export default function Page() {
  return <TeamDay />;
}
