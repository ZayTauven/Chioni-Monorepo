/*
 * Chioni — /plateforme/reconciliation : les incidents de paiement PSP.
 */
import { PlatformReconciliation } from '@/screens/plateforme/Reconciliation';

export default function Page() {
  return <PlatformReconciliation />;
}
