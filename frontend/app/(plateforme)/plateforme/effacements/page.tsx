/*
 * Chioni — /plateforme/effacements : la file des demandes d'effacement (RGPD).
 */
import { PlatformErasureRequests } from '@/screens/plateforme/ErasureRequests';

export default function Page() {
  return <PlatformErasureRequests />;
}
