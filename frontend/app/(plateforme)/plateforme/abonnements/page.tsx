/*
 * Chioni — /plateforme/abonnements : les contrats des tenants.
 */
import { PlatformSubscriptions } from '@/screens/plateforme/Subscriptions';

export default function Page() {
  return <PlatformSubscriptions />;
}
