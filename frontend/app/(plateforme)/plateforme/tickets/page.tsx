/*
 * Chioni — /plateforme/tickets : la file de support, tous tenants.
 */
import { PlatformSupportTickets } from '@/screens/plateforme/SupportTickets';

export default function Page() {
  return <PlatformSupportTickets />;
}
